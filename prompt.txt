Prompt

I am an intern at my school's cs internship programme. the internship has asked me to work on this project called 'wealth.wise'.
This project is a basic mock wealth management web app, that can be locally run (localhost ports).
To develop this project, I will be using:
* javascript (pupeteer for node.js) for web scrapping (scrapper folder).
* springboot for backend (API for frontend) (backend folder).
* react for frontend (frontend folder)
* MySQL for database (db folder) (I will be making the schema (schema.sql))

I want to target yahoo!finance for web scraping NIFTY 50 companies. the main scrapping code is in index.js, the list of 50 companies (from NIFTY) will be in companies.js and packages will be in package.json
the scrapped data will be transferred to the database
the database, and webscraping will be having these fields: 
["symbol", "name", "price", "change", "change_percent", "volume", "market_cap", "pe_ratio", "dividend_yield", "52_week_high", "52_week_low"]
Database will be normalised, and these are the tables and how they are related
i do not know how to do backend, what files to have, but it was instructed that i use springboot (java) for it.

the springboot (api) will be used to fetch data from the database and send it to the frontend
the frontend will be used to display the data in a tabular format.

Examine, analyse the design given in design.html in the frontend. try to recreate this. 
this is what the app needs:
/login              → Login page (just a form and then a button saying "start as a mock user", the mock user has virtual money for demo, no real auth needed for mock)
/dashboard          → Main screen — portfolio summary, total value, charts
/stocks             → Browse all 50 companies, search/filter
/stocks/:symbol     → Single stock page — price chart, buy/sell button
/portfolio          → User's holdings — what they own, P&L
/transactions       → History of buys/sells

I want to use tailwindcss for styling the frontend
I want to use mysql for database
I want to use springboot for backend
I want to use react for frontend
I want to use nodejs (pupeteer) for web scraping
I want the project to be able to run locally on localhost ports

tell me if you need anything, any packaes, any apps that need to be downloaded to run the project and i will download it.